# ============================================================
# TA-BNT / TissueFormer — Python dependencies
# ============================================================
# Pinned to the versions used on the cluster as of May 2026.
# Update via `pip install -r requirements.txt` and regenerate with
# `pip freeze > requirements.txt` after any environment change.

# ----- Core scientific stack -----
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
scikit-image>=0.21
statsmodels>=0.14

# ----- Neuroimaging -----
nilearn>=0.10
nibabel>=5.1

# ----- Deep learning -----
# Pin torch to the cluster's CUDA version when freezing; the line below
# accepts whatever is already installed.
torch>=2.0
torchvision>=0.15

# ----- Config / experiment management -----
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.15

# ----- 3D rendering for the interpretability figures -----
pyvista>=0.42
vtk>=9.2

# ----- Misc utilities -----
PyYAML>=6.0
tqdm>=4.65
openpyxl>=3.1   # for Benchmark.xlsx / Benchmark_filled.xlsx
python-docx>=1.0  # for the manuscript .docx files
